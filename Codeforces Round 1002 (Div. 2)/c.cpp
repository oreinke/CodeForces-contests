#include <bits/stdc++.h>

using namespace std;

int main() {
    int t; cin >> t;
    while (t--) {
        int n; cin >> n;
        vector<vector<int>> times (n, vector<int> (n));
        for (auto &x :times) {
            for (auto &y : x) cin >> y;
        }

        vector<int> sums (n, vector<int>)        
    }
}